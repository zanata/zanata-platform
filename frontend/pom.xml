<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.zanata</groupId>
    <artifactId>server</artifactId>
    <version>3.7.0-SNAPSHOT</version>
  </parent>
  <artifactId>frontend</artifactId>
  <name>frontend</name>
  <url>http://maven.apache.org</url>
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <node.version>v0.12.2</node.version>
    <npm.version>2.7.6</npm.version>
    <assets.target>${project.build.directory}/assets</assets.target>
    <bundle.output>${project.build.directory}/build</bundle.output>
    <bundle.dest>${bundle.output}/META-INF/resources</bundle.dest>
    <node.working.directory>${assets.target}</node.working.directory>
  </properties>


  <dependencies>

  </dependencies>

  <build>
    <resources>
      <resource>
        <filtering>false</filtering>
        <directory>src/main/javascript</directory>
        <targetPath>${assets.target}</targetPath>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>com.github.eirslett</groupId>
        <artifactId>frontend-maven-plugin</artifactId>
        <version>0.0.23</version>
        <configuration>
          <workingDirectory>${node.working.directory}</workingDirectory>
        </configuration>

        <executions>

          <execution>
            <id>install node and npm</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>install-node-and-npm</goal>
            </goals>
            <configuration>
              <nodeVersion>${node.version}</nodeVersion>
              <npmVersion>${npm.version}</npmVersion>
            </configuration>
          </execution>

          <execution>
            <id>npm install for all submodules</id>
            <phase>process-resources</phase>
            <goals>
              <goal>npm</goal>
            </goals>
            <!-- Optional configuration which provides for running any npm command -->
            <configuration>
              <arguments>install</arguments>
            </configuration>
          </execution>

          <execution>
            <id>npm build</id>
            <phase>compile</phase>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>run build bundleDest=${bundle.dest}</arguments>
            </configuration>
          </execution>

        </executions>
      </plugin>

      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <skip>true</skip>
          <failIfNoTests>false</failIfNoTests>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <configuration>
          <classesDirectory>${bundle.output}</classesDirectory>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!-- need to create one profile for each sub module (node and npm will be downloaded every time ... -->
    <profile>
      <id>user-profile-page</id>
      <properties>
        <node.working.directory>${assets.target}/user-profile-page</node.working.directory>
      </properties>
      <activation>
        <property>
          <name>userProfile</name>
        </property>
        <activeByDefault>true</activeByDefault>
      </activation>

    </profile>
  </profiles>
</project>

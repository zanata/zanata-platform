frontend.bundle.min.js
frontend.legacy.min.js

# The styleguide assumes all files will be in src/ so some generated files are output to src/ instead of build/
# TODO fix styleguide so that generated files do not have to be in /src
main/styles/atomic.css

# When *testing*, these values are used by Seam to resolve components.xml at 
# runtime.
# When *building* flies.war, the values in components.xml will be filtered 
# by Maven based on system properties (eg from the active Maven profile).

env.debug=true
env.import.testdata=true
env.server.path=http://localhost:8080/flies
env.dbunit.type=HSQL

# By default, we hot deploy all Action classes
#**/action/*Action.class

# RestEasy doesn't support hot deployment, so leaving this out for now
# **/rest/service/*Service.class

**/rest/service/*ActionImpl.class

# GWT action handlers
**webtrans/gwt/*Handler.class

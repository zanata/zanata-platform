/**
 * Path Definitions
 */

/* eslint-disable semi */

// FIXME use path.join in all cases

var paths = {};

paths.app = './app';
paths.build = './dist';
paths.config = paths.app + '/config.json';

module.exports = paths;
